{
  "name": "pigeonpost-store-adapter",
  "version": "0.1.0",
  "private": true,
  "description": "Fronts the MASAAS member billing backend for the Pigeonpost handle store. Mimics the theneuralledger adapter: proxy the customer's own token, MASAAS hosts payment.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/masaas.js && node --check src/config.js"
  },
  "engines": { "node": ">=18" }
}
