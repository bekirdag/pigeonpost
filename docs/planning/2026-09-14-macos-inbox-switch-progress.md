# macOS inbox switch repair progress — 2026-09-14

## Header simplification follow-up

- Removed the permanent current-address/copy row from the Mac inbox sidebar as requested; the mailbox dropdown and Settings retain their copy controls. Loading, retry and mailbox-switch behavior are unchanged. Docdex symbols and impact were inspected; this change removes one isolated view expression.
- Existing native window integration passes all 21 checks after the change; inspected its captured window to confirm the compact sidebar header. Universal Developer ID Release archive 39 succeeded, with both architectures, hardened runtime and no debugger entitlement verified. Publication and notarization are next. Evidence: `/private/tmp/pigeonpost-mac-header-audit-20260914`.
- Published `macos-1.0-39` from `0ed2d1acec678021ebba01134ca21c3e7daff437`. Notarization run `34835266503` accepted submission `758803f5-1795-4b16-92b4-67d853cccac4`; the public ZIP and checksum were downloaded without authentication and fully verified. Final SHA-256: `9f6898dfa2284b1059ca54d49036f5c426ad2fc59b8f1845b3a73738a52afc64`; 1,520,941 bytes. The Mac download card now points to 39. Docdex does not index HTML symbols, so the narrow card edit is checked directly and through existing download tests; no platform-selection code changed.
- Updated the download tests' pinned Mac URL from 37 to 39; all 23 existing OS recommendation, manual platform choice, icon and homepage-link checks pass after installing the worktree's locked test dependencies. Installed the public notarized 39 package into both local Applications locations after a normal quit, keeping verified build 38 ZIP backups under `~/Library/Application Support/PigeonpostDesktop/UpdateBackups/20260914-build39/`. Reopened the personal copy and inspected its authenticated inbox: the duplicate address/copy row is gone and conversations are loaded.

- Main was clean at `880d245104b3a034930155eb77c0e567dba0ea36`; created isolated branch `codex/mac-inbox-switch-20260914`.
- Read Pigeonpost messaging and planning skills, both Docdex lobes and wakeup; saved preference for responsive inbox switching with visible loading feedback. Existing user release/push/deployment authorization satisfies inferred operator-routine gates.
- Linux test thread `fb00d423f05941d2a42a2d42f2be89bc` contains only the original outgoing installation request. The peer's other threads have no newer activity. No reply observed; no repeat request or permission changes made.
- Current macOS source uses an unkeyed `.task` for both initial load and live polling. Mailbox picker and peer visit clear Inbox but do not restart that task. Shared async loaders/poller do not reject responses for a previously selected mailbox; sidebar has no spinner/empty state. These are concrete defects; runtime reproduction and regression tests are next.
- Refreshed the index for the relevant Swift files. Impact tools return no resolved Swift edges, so dependency scope is checked manually: macOS and iOS both consume shared Inbox; API requests use URLSession. DAG trace retrieved for change ordering.
- Added deterministic URLSession tests compiling the real Inbox and PostboxClient. Baseline: 61 checks, 16 failures (stale mailbox data/metadata, wrong loading flags, cancellation, A → B → A, stale 401). After the fix: 61 checks, zero failures; existing authentication, conversation/Markdown and 66 purchase-controller checks pass too. Local-model helper draft used invalid Foundation APIs and was discarded; the replacement compiled and reproduced the baseline defects.
- macOS now runs loading/live polling in a task keyed to the selected identity, independently of notification authorization. Both visit actions clear navigation synchronously. Native spinner/status, selected address, empty state and failure/retry views are implemented. Shared responses are guarded by captured identity and mailbox generation, including error paths; only successful inbox responses mark data loaded.
- Debug macOS build succeeded. Native NSHostingView integration and iOS compatibility build are in progress. Tests run through Docdex with `DOCDEX_RUN_TESTS_CMD=sh` and the script passed as target because this worktree has no configured test runner.
- Native integration now passes 21 checks with the actual MacInboxView and Inbox, isolated sheets/notification doubles and controlled URLSession. Switching starts the new load in under one second, cancels old requests, and loads even while notification authorization is pending. Failure/retry and successful empty loading pass; captured and inspected actual window screenshots. Corrected the empty fixture to contain no contacts (known contacts intentionally create conversation rows). iOS simulator compatibility build passed.
- A second window on the same identity no longer resets the shared mailbox generation; only actual identity changes prepare a fresh inbox. Final model run remains 61/61 passed. Runtime changes are ready for signed universal archive, notarization and download publication.

## Release and rollout

- Source `c3ebbf017566190e8018bdb6cb8fb859f9365d16`, merged through PR #17 as `6326d5dec82460d65878bcfa8aef5b10256ac1ed`. Final iOS simulator compatibility build succeeded after the shared-model changes.
- Published `macos-1.0-38`, a universal Developer ID build signed by Wodo with hardened runtime and no debugger entitlement. Notarization run `34832680324` accepted submission `fa65e796-d7ab-4069-a4f7-470c5abc48c5`; stapling and Gatekeeper validation passed.
- Downloaded the public ZIP and checksum without authentication and reran the full package verifier. Verified SHA-256: `abb918a4f6a78c32241b5912e64ef27b0e595b9a1da6176d93957a7eb89aed2a`; 1,522,187 bytes; macOS 14 minimum; both arm64 and x86_64 verified. All three public release/download/checksum links return HTTP 200.
- Updated the Mac download card to build 38 and corrected its stale visible version/size label. This one-file website rollout uses the previous deployed hash as a guard and backs up the original at `/var/backups/pigeonpost/mac-inbox-20260914`; older public app archives remain available for rollback.
- General CI `34832614974` has passed macOS/Linux tests, lint, audit, delivery/privacy, custody, web and npm checks. The unchanged Windows Rust test job was still running at merge; no required branch protection was bypassed. Final status is retained in the private audit.
- Linux request rechecked after Mac notarization: the thread still contains only the original outgoing request, with no reply. No repeat message or permission changes were sent.
- Private evidence: `/private/tmp/pigeonpost-mac-inbox-audit-20260914`, including baseline/final model logs, native integration log and window screenshots, signed/public package verification, notarization report and deployment/synchronization evidence.

## Final verification

- All 12 PR checks finished successfully, including Windows. The deployed `/download` and `/download.html` both return build 38 with identical verified HTML hashes; CSP and platform selection are preserved. Four production Git checkouts and local main/release worktree were clean and synchronized after deployment.
- Found two installed app copies: `/Applications` had build 37, while the running personal Applications copy was an older debug build 1. Sampling the latter showed an idle AppKit event loop rather than a CPU-bound deadlock; its visible compose field was empty. Quit it normally, verified persistent ZIP backups of both original applications, installed the notarized build 38 into both locations, revalidated Gatekeeper, and reopened the personal copy. Account data and Keychain entries were not modified by the installer.
- Local backups: `~/Library/Application Support/PigeonpostDesktop/UpdateBackups/20260914-build38/`. Installation and running-process evidence are in `local-app-updates.json` and `running-updated-app.json` in the private audit folder.
